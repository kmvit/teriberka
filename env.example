# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
# Для SQLite (по умолчанию)
# DATABASE_URL не требуется

# Для PostgreSQL (если нужно)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Frontend URL (для ссылок восстановления пароля)
FRONTEND_URL=http://localhost:3000

# Email Settings (если используется)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@teriberka.com

# Т-Банк Эквайринг (T-Bank Payment Gateway)
# Получите ключи в личном кабинете интернет-эквайринга Т-Банка
TBANK_TERMINAL_KEY=your_terminal_key_here
TBANK_PASSWORD=your_password_here
TBANK_API_URL=https://securepay.tinkoff.ru/v2

# Backend URL (для webhook)
# Локально с ngrok: https://your-ngrok-id.ngrok.io
# Продакшен: https://your-domain.com
BACKEND_URL=https://your-ngrok-id.ngrok.io

# Webhook URL для уведомлений от Т-Банка (формируется автоматически из BACKEND_URL)
# Можно переопределить вручную если нужно
# TBANK_NOTIFICATION_URL=https://your-ngrok-id.ngrok.io/api/v1/payments/webhook/

# URLs для перенаправления после оплаты (frontend)
PAYMENT_SUCCESS_URL=http://localhost:3000/payment/success
PAYMENT_FAIL_URL=http://localhost:3000/payment/fail

# Telegram Bot для уведомлений о бронированиях
# Создайте бота через @BotFather в Telegram
# Добавьте бота администратором в ваш канал/группу
# Для получения ID канала/группы используйте @userinfobot или отправьте сообщение и получите через API
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHANNEL_ID=-1001234567890
# Для группы: числовой ID (например: -1001234567890)
# Для канала: @channel_username или числовой ID (например: -1001234567890)

# SMS.RU для регистрации по телефону
# API ключ из личного кабинета sms.ru (https://sms.ru)
SMS_RU_API_ID=your_api_id_here
# SMS_RU_TEST=True

# reCAPTCHA v2 - защита от ботов при запросе SMS-кода
# Создайте ключи на https://www.google.com/recaptcha/admin
RECAPTCHA_SECRET_KEY=your_secret_key_here
# Для frontend (в frontend/.env): VITE_RECAPTCHA_SITE_KEY=your_site_key_here

# Google Calendar для синхронизации бронирований
# 1. Создайте Service Account в Google Cloud Console (https://console.cloud.google.com/)
# 2. Включите Google Calendar API для вашего проекта
# 3. Создайте Service Account и скачайте JSON файл с credentials
# 4. Поделитесь календарем админа с email Service Account (дайте права "Make changes to events")
# 5. Укажите путь к JSON файлу и email календаря ниже
GOOGLE_CALENDAR_SERVICE_ACCOUNT_FILE=/path/to/service-account.json
GOOGLE_CALENDAR_ID=admin@example.com
# GOOGLE_CALENDAR_ID - это email календаря админа, куда будут отправляться события
